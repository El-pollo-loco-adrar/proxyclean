---
import Layout from "../layouts/Layout.astro";
---

<Layout title="OUPS - Erreur 404" description="Vous n'auriez pas dû venir ici...">
    
    <main class="min-h-screen bg-pink-500 overflow-hidden relative flex flex-col items-center justify-center p-4">

        <div class="z-10 text-center space-y-8 bg-yellow-300 p-8 border-8 border-dashed border-red-600 shadow-[20px_20px_0px_0px_rgba(0,0,0,1)] rotate-2 max-w-2xl w-full">
            
            <h1 class="text-6xl md:text-9xl font-black text-red-600 italic uppercase animate-bounce">
                OUUUUPS !
            </h1>

            <div class="space-y-4">
                <p class="text-2xl font-bold text-black uppercase tracking-tighter">
                    Vous êtes perdu ? C'est normal.
                </p>
                <p class="text-xl font-mono bg-black text-green-400 p-2 inline-block">
                    Erreur 404 : Trop de propreté tue la propreté.
                </p>
            </div>

            <div class="bg-white p-4 border-4 border-black shadow-[8px_8px_0px_0px_rgba(0,0,0,1)]">
                <p class="font-bold mb-4 uppercase tracking-widest text-red-600 animate-pulse">
                    🔈 MONTE LE SON, POTO ! 🔈
                </p>
                
                <div class="rounded-lg overflow-hidden border-2 border-gray-200 bg-gray-50">
                    <iframe 
                        src="https://share-ai.singgenix.com/?token=IyHubPlbAD5C6a2GS93Dnxp%2BiWV3r5%2F7JdHU2jcGcayeIr9%2Be8svRA%3D%3D&musicType=0" 
                        width="100%" 
                        height="200" 
                        frameborder="0" 
                        allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture" 
                        loading="lazy">
                    </iframe>
                </div>
                
                <p class="text-[10px] text-gray-400 mt-2 italic">Si la musique ne démarre pas, clique sur le bouton "Play" ci-dessus.</p>
            </div>

            <a href="/" class="btn btn-lg bg-red-600 text-white border-none hover:bg-black transition-all animate-pulse px-8 py-4 text-xl font-black">
                FUIR CETTE PAGE
            </a>
        </div>

        <div class="fixed inset-0 pointer-events-none z-0 mix-blend-overlay opacity-20">
            <marquee scrollamount="15" class="text-9xl font-black text-white">404 404 404 404 404 404 404 404</marquee>
        </div>

    </main>
</Layout>

<style>
    /* Effet de flash sur le fond */
    main {
        animation: flashBg 0.5s infinite alternate;
    }
    @keyframes flashBg {
        from { background-color: #ec4899; }
        to { background-color: #a855f7; }
    }

    /* Style pour le texte clignotant */
    .animate-pulse {
        animation: pulse 0.5s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    }
    @keyframes pulse {
        0%, 100% { opacity: 1; }
        50% { opacity: .5; }
    }
</style>